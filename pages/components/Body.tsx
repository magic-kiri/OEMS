import React from "react";

const Body = () => {
  return (
    <div
      style={{
        display: 'flex',
        justifyContent: 'space-between',
        backgroundColor: "green",
        minHeight: "100px",
      }}
    ></div>
  );
};

export default Body;
